# Homepage Context Pointer

`homepage/` is a legacy/static homepage area, not the active Running Page 2.0 app.

Default agents should work in `apps/web/` and read:

- ../docs/agentic/INDEX.md

Only inspect this folder when the task explicitly mentions the static homepage.

