# Agent Entry

This repository is maintained through agentic coding. Keep this entry thin.

Start with [docs/agentic/INDEX.md](docs/agentic/INDEX.md), then read only the context packs named for the current task.

Default rules:
- Do not read `docs/archive/` unless the task explicitly asks for historical context.
- Do not load large generated assets or data files unless the task is about sync/data repair.
- Treat `docs/agentic/` as the source of truth for project context.
- Prefer small, staged edits with build verification before commit/push.
- Preserve the current product direction: restrained, data-first UI with map and trajectory features intact.

